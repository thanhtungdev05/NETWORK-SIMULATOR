# Auto detect text files and perform LF normalization
* text=auto

# SQL migrations must stay LF so checksums match across Windows/Linux
*.sql text eol=lf

# Shell scripts must stay LF for Docker/Linux compatibility
*.sh text eol=lf
